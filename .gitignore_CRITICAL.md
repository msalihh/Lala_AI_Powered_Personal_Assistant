# ⚠️ KRİTİK: Git Commit Öncesi Kontrol Listesi

## ❌ ASLA COMMIT ETMEYİN:

### 1. Private Keys ve Secrets
- `.android/adbkey` ❌
- `.android/adbkey.pub` ❌
- `*.key`, `*.pem`, `*.p12` ❌
- `id_rsa*`, `id_dsa*` ❌
- `*.keystore` ❌

### 2. Kişisel Dosyalar
- `.bash_history` ❌
- `.cache/` ❌
- `.huggingface/` ❌

### 3. Büyük Cache Dosyaları
- HuggingFace model cache ❌
- ChromaDB data ❌
- Node modules ❌

## ✅ SADECE ŞUNLARI COMMIT EDİN:

- ✅ Source code (`.py`, `.ts`, `.tsx`, `.js`)
- ✅ Configuration files (`package.json`, `requirements.txt`)
- ✅ Documentation (`README.md`)
- ✅ `.gitignore`
- ✅ Project structure

## 🔒 Güvenlik Kontrolü

Eğer private key commit edildiyse:
1. **HEMEN** GitHub'da repository'yi private yapın
2. Private key'i değiştirin/revoke edin
3. Git history'den temizleyin (git filter-branch veya BFG Repo-Cleaner)

